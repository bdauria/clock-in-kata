const { Observable } = require('rxjs');

module.exports = {
  commitTime,
  getPosition
};

function commitTime(user, position = null) {
  throw new Error('not implemented yet.');
}

function getPosition() {
  throw new Error('not implemented yet.');
}
